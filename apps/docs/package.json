{
  "name": "@sandbox/docs",
  "version": "0.8.0-beta.1",
  "private": true,
  "scripts": {
    "generate:references": "tsx ../../scripts/generate-docs-reference.ts",
    "predev": "npm run generate:references",
    "dev": "mint dev --port 3200 --no-open",
    "prebuild": "npm run generate:references",
    "build": "mint validate",
    "check:links": "mint broken-links --check-anchors --check-redirects",
    "check:a11y": "mint a11y",
    "start": "mint dev --port 3200 --no-open"
  },
  "devDependencies": {
    "mint": "4.2.851",
    "openapi-types": "12.1.3",
    "tsx": "^4.20.5",
    "typescript": "~5.9.3"
  }
}
